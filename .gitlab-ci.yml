image: "rust:latest"

before_script:
  - rustup default nightly
  - cargo install cargo-make

stages:
  # - build
  - verify
  # - coverage

# build:
#   stage: build
#   script:
#     - cargo make build

verify:
  stage: verify
  script:
    - apt update
    - apt install firefox-esr -y
    - rustup component add rustfmt
    - cargo make verify
# coverage:
#   stage: coverage
#   script:
#     - cargo make coverage
